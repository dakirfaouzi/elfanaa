{
  "name": "@platform/db",
  "version": "0.0.0",
  "private": true,
  "description": "Schema-host package for the Fanaa Platform Prisma DB. Owns prisma/schema.prisma and migrations. JS exports are intentionally absent in M1 — apps still import directly from @prisma/client. JS facade lands in M2 when apps/studio needs a shared client.",
  "files": [
    "prisma"
  ],
  "exports": {
    "./schema": "./prisma/schema.prisma"
  },
  "dependencies": {
    "@prisma/client": "^6.19.3"
  },
  "devDependencies": {
    "prisma": "^6.19.3"
  }
}
